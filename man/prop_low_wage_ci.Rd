% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prop_low_wage_ci.R
\name{prop_low_wage_ci}
\alias{prop_low_wage_ci}
\title{Calculate Weighted Proportion of Low-Wage Jobs with Confidence Interval}
\usage{
prop_low_wage_ci(
  data,
  weight_var = "gewicht",
  group_var = c(""),
  CV_min = 5,
  Konfidenz = qnorm(0.975)
)
}
\arguments{
\item{data}{A data frame containing the variables.}

\item{weight_var}{Character. Name of the weight variable. Default is "gewicht".}

\item{group_var}{Character vector. Names of grouping variables. Default is c("").}

\item{CV_min}{Numeric. Minimum acceptable coefficient of variation. Default is 5.}

\item{Konfidenz}{Numeric. Z-score for confidence interval. Default is qnorm(0.975).}
}
\description{
This function calculates the weighted proportion of low-wage jobs within specified groups,
including confidence intervals and flags for small sample sizes or high coefficients of variation.
}
\examples{
\dontrun{
# Example data
DATA <- data.frame(
  age = c(25, 32, 45, 55, 67),
  gender = c("Male", "Female", "Female", "Male", "Female"),
  gewicht = c(1.2, 0.8, 1.5, 1.0, 0.9),
  mbls = c(1200, 900, 1500, 2000, 1000)
)

DATA \%>\%
  dplyr::mutate(age_group = dplyr::case_when(
    age < 20 ~ "Under 20",
    age < 30 ~ "20-29",
    age < 40 ~ "30-39",
    age < 50 ~ "40-49",
    age < 65 & gender == "Female" ~ "50-64/65",
    age < 66 & gender == "Male" ~ "50-64/65",
    age >= 62 ~ "65+"
  )) \%>\%
  LSEmetrics::prop_low_wage_ci(
    weight_var = "gewicht",
    group_var = c("age_group"),
    CV_min = 10
  )
}

}
